eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:    
  h2:
    console:
      enabled: true
  zipkin:
    base-url: http://127.0.0.1:9411/
